import numpy as np


arr_a = np.random.randint(0,50,10)
arr_b = np.random.randint(0,30,10)

#+ - *  ** each number on the same index but the shapes (last number) needs to be the same size
arr_c = arr_a * arr_b
#adding 100 to every number in array 
arr_d = arr_a + 100
